targets:
  $default:
    builders:
      drift_dev:
        options:
          # The directory where the schema files are stored:
          schema_dir: lib/database/schemas/  # (default)

          databases:
            # Required: A name for the database and its path
            my_database: lib/database/app_database.dart

          